# Google Cloud Credentials

## ⚠️ QUAN TRỌNG - BẢO MẬT

Thư mục này chứa các file credentials cho Google Cloud Vertex AI.

### 🔒 Bảo mật
- ❌ **KHÔNG BAO GIỜ** commit các file `.json` lên Git
- ❌ **KHÔNG BAO GIỜ** share file credentials trên public
- ✅ File này đã được thêm vào `.gitignore`

### 📁 Cách sử dụng

1. **Tải file JSON key từ Google Cloud Console**
   - Vào IAM & Admin → Service Accounts
   - Tạo key cho service account
   - Download file JSON

2. **Đặt file vào thư mục này**
   ```
   credentials/
     ├── README.md (file này)
     └── vertex-ai-key.json (file key của bạn)
   ```

3. **Đổi tên file (nếu cần)**
   - Đổi tên thành: `vertex-ai-key.json`
   - Hoặc giữ nguyên tên và update path trong environment variable

4. **Set environment variable**
   ```bash
   # Windows PowerShell
   $env:GOOGLE_APPLICATION_CREDENTIALS="D:\Doan\Khoa_Luan_Tot_Nghiep\SportifyBackend\credentials\vertex-ai-key.json"
   
   # Linux/Mac
   export GOOGLE_APPLICATION_CREDENTIALS="/path/to/credentials/vertex-ai-key.json"
   ```

### 📝 Lưu ý
- File credentials không được tracked bởi Git
- Mỗi developer cần có file credentials riêng
- Không share credentials giữa các môi trường (dev/staging/prod)

### 🔗 Tham khảo
Xem hướng dẫn đầy đủ tại: `VERTEX_AI_SETUP.md`
